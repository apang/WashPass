Ensure only registered members can access the redemption flow.
Work Plan:
Configure Amazon Cognito or Auth0 for user sign-up and login.
Protect Student B's API endpoints using JWT (JSON Web Token) validation.
Add a "User Profile" section to Student C’s frontend to display the logged-in user's status.
Implement a mock "Subscription Check" to ensure the user is an active member.
